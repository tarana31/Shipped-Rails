<p id="notice"><%= notice %></p>

<h1>Boats</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Max containers</th>
      <th>Location</th>
      <th>Description</th>
      <th>Contact</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @boats.each do |boat| %>
      <tr>
        <td><%= boat.name %></td>
        <td><%= boat.max_containers %></td>
        <td><%= boat.location %></td>
        <td><%= boat.description %></td>
        <td><%= boat.contact %></td>
        <td><%= link_to 'Details', boat %>
        <%= link_to 'Edit', edit_boat_path(boat) %>
        <%= link_to 'Delete', boat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Boat', new_boat_path %>
