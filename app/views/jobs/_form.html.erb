<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :cost %>
    <%= form.text_field :cost, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :containers_needed %>
    <%= form.number_field :containers_needed, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :boat_id %>
    <%= form.number_field :boat_id, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, class: "form-control" %>
  </div>

  <div class="actions form-group">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
